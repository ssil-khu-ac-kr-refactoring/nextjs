generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Research {
  id          String         @id @default(cuid())
  title       String
  description String?
  status      ResearchStatus @default(IN_PROGRESS)
  startDate   DateTime?
  endDate     DateTime?
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  imageUrl    String?
  contentHtml String?
  published   Boolean        @default(false)
  order       Int            @default(0)
}

model News {
  id          String   @id @default(cuid())
  title       String
  description String
  imageUrl    String?
  publishedAt DateTime @default(now())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  contentHtml String?
}

model Person {
  id          Int      @id @default(autoincrement())
  name        String
  position    String
  description String?
  image       String?
  email       String   @unique
  degree      String?
  role        Role
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model HomePageContent {
  id             Int    @id @default(1)
  heroTitle      String
  heroSubtitle   String
  heroParagraph  String
  aboutTitle     String
  aboutParagraph String
  newsTitle      String
  newsSubtitle   String
  fontFamily   String?  
}

model SliderImage {
  id        Int      @id @default(autoincrement())
  imageUrl  String
  altText   String?
  order     Int      @default(0)
  createdAt DateTime @default(now())
}

model Asset {
  id           String         @id @default(cuid())
  url          String
  originalName String?
  mime         String?
  size         Int?
  ownerType    AssetOwnerType
  ownerId      String
  tag          String?
  createdAt    DateTime       @default(now())

  @@index([ownerType, ownerId])
}

model Contact {
  id          Int      @id @default(autoincrement())
  labNameKo   String
  labNameEn   String
  addressKo   String
  addressEn   String
  mapEmbedUrl String
  updatedAt   DateTime @updatedAt
}

model AboutContent {
  page       String   @id
  data       Json
  updatedAt  DateTime @updatedAt
}

model Publication {
  id        Int      @id @default(autoincrement())
  title     String
  authors   String
  venue     String?
  year      Int
  month     Int?
  url       String?
  pdfUrl    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([year], map: "idx_publication_year")
}

model BoardTab {
  id          Int         @id @default(autoincrement())
  name        String      @unique
  slug        String      @unique
  description String?
  order       Int         @default(0)
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  posts       BoardPost[]
}

model BoardPost {
  id          String    @id @default(cuid())
  title       String
  description String
  contentHtml String?
  imageUrl    String?
  published   Boolean   @default(false)
  publishedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  tabId       Int
  tab         BoardTab  @relation(fields: [tabId], references: [id])
}

model SimulationRow {
  id       Int    @id @default(autoincrement())
  form     String
  node0Mat String @map("node0_mat")
  timeMode String @map("time_mode")
  lat      Float
  lon      Float
  avPot    Float  @map("av_pot")
  nth      Float
  tth      Float
  ne       Float
  te       Float
  ni       Float
  ti       Float
  alt      Float
  sey      Float?
  mpd      Float?
  pey      Float?
  ipe      Float?
  pee      Float?
  msey     Float?
  buc      Float?
  sre      Float?

  @@index([form, node0Mat, timeMode])
  @@map("simulation_rows")
}

model PotentialMatrix {
  id         Int    @id @default(autoincrement())
  form       String
  node0Mat   String @map("node0_mat")
  potentials Json

  @@unique([form, node0Mat])
  @@map("potential_matrix")
}

enum ResearchStatus {
  IN_PROGRESS
  COMPLETED
}

enum Role {
  PROFESSOR
  CURRENT
  ALUMNI
}

enum AssetOwnerType {
  RESEARCH
  NEWS
}
